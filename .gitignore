__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
.DS_Store
*.ipynb_checkpoints
.pytest_cache/
.mypy_cache/
.ruff_cache/
dist/
build/

# Colab notebook runtime artifacts — generated at the repo root inside the
# cloned repo on the Colab VM (relative to whatever cwd the notebook %cd'd
# into), never meant to be committed.
stage0_results.json
*_checkpoint.jsonl
